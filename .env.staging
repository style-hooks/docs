# This can safely reside in the git repo.
# Don't be an idiot and fill it with secure info.
DOMAIN='staging-style-hooks.jaredlunde.com'
# The sets the 'publicPath' option for Webpack. It must end with a trailing slash. If
# using CloudFront, be sure to change this value to the CloudFront domain.
PUBLIC_PATH='https://lunde-staging-cloud.s3.amazonaws.com/'
# Serverless Framework config
SERVERLESS__PROFILE='default'
SERVERLESS__SITE_S3_BUCKET='staging-style-hooks.jaredlunde.com'
SERVERLESS__HOSTED_ZONE='jaredlunde.com.'
SERVERLESS__CLIENT_S3_BUCKET='lunde-staging-cloud'